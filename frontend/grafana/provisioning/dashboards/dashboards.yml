apiVersion: 1
providers:
  - name: "FLASHES Dashboards"
    folder: "FLASHES"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
